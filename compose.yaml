services:
  client:
    build:
      context: ./client
    ports:
      - "4200:4200"
    depends_on:
      - server

  server:
    build:
      context: ./server
    env_file:
      - .env
    ports:
      - "8000:8000"
